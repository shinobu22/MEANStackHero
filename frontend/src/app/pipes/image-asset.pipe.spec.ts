import { ImageAssetPipe } from './image-asset.pipe';

describe('ImageAssetPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageAssetPipe();
    expect(pipe).toBeTruthy();
  });
});
